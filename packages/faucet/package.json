{
  "name": "@tdea/faucet",
  "version": "1.0.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "lint": "eslint . --ext .ts,.tsx --max-warnings=0",
    "build": "next build" 
  },
  "dependencies": {
    "@tdea/algorand-utils": "*",
    "react-qr-code": "^2.0.15"
    /* TODAS las demás dependen. cias (React, Next, Lucide, Zod, hookform, Radix, etc.) 
       han sido ELIMINADAS de aquí y ahora DEBEN estar instaladas SÓLO en la raíz. 
       Esto resuelve el conflicto de useActionState y Lucide-React. */
  },
  "devDependencies": {
    /* Mantenemos solo el mínimo necesario si no se migra a la raíz */
    "typescript": "^5.4.0",
    "eslint": "^9.0.0"
  },
  "peerDependencies": {
    /* Este bloque se elimina para evitar la doble instalación de React */
  }
}
