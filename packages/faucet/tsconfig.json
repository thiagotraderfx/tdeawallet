{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "composite": false,
    // ¡CAMBIO CRÍTICO! Usar la resolución de módulos más moderna y flexible de Node.js
    "moduleResolution": "nodeNext", 
    "resolveJsonModule": true,
    "isolatedModules": true,
    "paths": {
      // 1. Alias Local (sin cambios)
      "@/*": ["./src/*"], 
      
      // 2. Alias Compartido (sin cambios)
      "@tdea/*": ["../../src/*"],
      
      // 3. Hack Genérico FINAL: Forzar que cualquier subpaquete de Genkit se resuelva
      // en la carpeta node_modules de la raíz del monorepo.
      "genkit/*": ["../../node_modules/genkit/*"]
    }
  },
  "include": [
    "src/**/*.ts",
    "src/**/*.tsx",
    "pages/**/*.ts",
    "pages/**/*.tsx",
    "../../src/ai/**/*.ts"
  ],
  "exclude": ["node_modules", "dist"],
  "references": [
    { "path": "../algorand-utils" }
  ]
}
